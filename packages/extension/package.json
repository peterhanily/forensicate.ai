{
  "name": "@forensicate/extension",
  "version": "1.0.0",
  "description": "Browser extension for scanning text with Forensicate.ai",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:icons": "node scripts/generate-icons.js",
    "build": "npm run build:all",
    "build:all": "npm run build:chrome && npm run build:firefox",
    "build:chrome": "npm run generate:icons && BROWSER=chrome node scripts/build.js",
    "build:firefox": "npm run generate:icons && BROWSER=firefox node scripts/build.js",
    "package": "npm run package:chrome && npm run package:firefox",
    "watch": "node scripts/build.js --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "package:chrome": "cd dist/chrome && zip -r ../forensicate-chrome.zip .",
    "package:firefox": "cd dist/firefox && zip -r ../forensicate-firefox.zip ."
  },
  "dependencies": {
    "@forensicate/scanner": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "@vitest/ui": "^4.1.11",
    "archiver": "^8.0.0",
    "esbuild": "^0.28.2",
    "jsdom": "^29.1.1",
    "sharp": "^0.35.4",
    "vitest": "^4.1.11"
  },
  "author": "Forensicate.ai",
  "license": "Apache-2.0"
}
